// const express = require("express");
// const router = express.Router();
// const { createChain, getChain, deleteChain } = require("../controllers/chain.controller");
// const authenticateToken = require("../middleware/auth");

// router.post("/:workspaceId/chain", authenticateToken, createChain);
// router.get("/:workspaceId/chain", authenticateToken, getChain);
// router.delete("/:workspaceId/chain", authenticateToken, deleteChain);

// module.exports = router;
